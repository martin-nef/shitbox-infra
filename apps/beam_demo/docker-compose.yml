services:
  web:
    image: ${APP_IMAGE}
    ports:
      - "127.0.0.1:80:80"
      - "127.0.0.1:443:443"
    env_file: .env
    restart: unless-stopped
    volumes:
      - ./.thruster:/rails/storage/thruster
